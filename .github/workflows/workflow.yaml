
on:
 push:
   branches:
     -main
jobs:
  build:

    runs-on:ubuntu-latest

  steps:

    -name: Checkout Code
    uses: actions/checkout@v4

    -name: on:
  push:
    branches:
      - main

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: login docker hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: build image and push to dockerhub
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: kumar2167/demo-nginx:latestlogin docker hub
      useson:
          push:
                branches:
                        - main

                jobs:

                    build:
                          runs-on: ubuntu-latest

                              steps:
                                    - name: Checkout code
                                          uses: actions/checkout@v4

                                              - name: login docker hub
                                                    uses: docker/login-action@v3
                                                          with:
                                                                    username: ${{ secrets.DOCKERHUB_USERNAME }}
                                                                            password: ${{ secrets.DOCKERHUB_TOKEN }}

                                                                                - name: build image and push to dockerhub
                                                                                      uses: docker/build-push-action@v5
                                                                                            with:
                                                                                                      context: .
                                                                                                              push: true
                                                                                                                      tags: kumar2167/demo-nginx:lateston:
  push:
    branches:
      - main

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: login docker hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USER }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: build image and push to dockerhub
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ajeeshregi/allianz-repo:nginx-allianz
